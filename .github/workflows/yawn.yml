name: worker

on:
  push:

jobs:
  miss:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        shoot: ["odour", "group", "era", "bundle", "indirect"]
        bean: ["hi", "companion", "chip", "simplicity"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar xx
